---
title: "Submit Flux Kontext Max task"
description: "POST Flux Kontext Max high-quality text-to-image"
openapi: "POST /api/v1/task/submit/flux-kontext-max"
---

## Submit task

- Send `Authorization: Bearer <your_api_key>`.
- Required: `prompt`. Optional: `aspect_ratio`, `image_urls` (max 4), `output_format`, `prompt_upsampling`, `safety_tolerance`, `seed`, `webhook_url`, `webhook_secret`.
- Async `task_id`; poll `/api/v1/task/status`.
- **Breaking:** `input_image`/`input_image_2`/`_3`/`_4` deprecated; use `image_urls` array.

```bash
curl -X POST https://api.wizzx.ai/api/v1/task/submit/flux-kontext-max \
  -H "Authorization: Bearer <api_key>" \
  -H "Content-Type: application/json" \
  -d '{
    "prompt": "A serene mountain landscape with a crystal clear lake",
    "aspect_ratio": "21:9",
    "image_urls": [
      "https://example.com/reference1.jpg"
    ],
    "output_format": "png",
    "safety_tolerance": 2,
    "prompt_upsampling": true
  }'
```

<Note>
- Cost: base 20-28 credits by aspect ratio; extra refs (2+) +5 credits each.  
- Aspect ratios 21:9 to 9:21 (`width:height`).  
- References: up to 4 (experimental).  
- Output: `jpeg`/`png` (default `png`).  
- Safety: 0-6 (0 strict, 6 lax), default 2.
</Note>
